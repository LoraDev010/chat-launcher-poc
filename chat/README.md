# Chat (POC)

Proyecto mínimo para chat en tiempo real (salas públicas, alias sin registro).

Run (server)

```bash
cd server
npm install
npm run start
```

Run (client)

```bash
cd client
npm install
npm run dev
```

Por defecto el cliente se conecta a `http://localhost:3000`.
